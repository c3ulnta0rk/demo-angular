import { DropdownActivatorDirective } from './dropdown-activator.directive';

describe('DropdownActivatorDirective', () => {
  it('should create an instance', () => {
    const directive = new DropdownActivatorDirective();
    expect(directive).toBeTruthy();
  });
});
