import { C3AutocompleteDirective } from './autocomplete.directive';

describe('AutocompleteDirective', () => {
  it('should create an instance', () => {
    const directive = new C3AutocompleteDirective();
    expect(directive).toBeTruthy();
  });
});
